/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['m.media-amazon.com' , 'www.independent.co.uk' ,'imgs.search.brave.com']
    }
};

export default nextConfig;
